export default function Loading() {
  return <main className="system-state page-shell" role="status" aria-live="polite"><span className="eyebrow">Loading</span><h1>Reading the course map…</h1><div className="system-state-skeleton" aria-hidden="true" /></main>;
}
